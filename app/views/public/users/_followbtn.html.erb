<% if current_user != user %>
  <% if user.followed_by?(current_user) %>
    <%= link_to "フォロー済み",user_relationship_path(user.id,follow.id), method: :delete, remote: true, class: "btn btn-outline-info" %>
  <% else %>
    <%= link_to "フォローする",user_relationships_path(user.id), method: :post, remote: true, class: "btn btn-outline-info" %>
  <% end %>
<% end %>